import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  base: {
    fontWeight: "bold",
    textAlign: "center",
    marginBottom: 10,
  },
});

export const titleSizes = StyleSheet.create({
  s: { fontSize: 18 },
  m: { fontSize: 22 },
  l: { fontSize: 26 },
  xl: { fontSize: 30 },
});
